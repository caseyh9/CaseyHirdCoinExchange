How to run the app:

1. Open a terminal and navigate to "Backend" directory.

2. Execute "./mvnw spring-boot:run" to start spring-boot backend.

3. Open a new terminal and navigate to "frontend" directory.

4. Execute "yarn start" to start react front end.

5. Go to http://localhost:3000/ to view the app.